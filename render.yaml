services:
  - type: web
    name: book-review-api
    env: python
    buildCommand: curl -LsSf https://astral.sh/uv/install.sh | sh && export PATH="$HOME/.cargo/bin:$PATH" && uv pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host